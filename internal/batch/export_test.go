package batch

// DefaultMaxSize exposes the default max size applied when Config.MaxSize <= 0.
const DefaultMaxSize = 100

// DefaultMaxWait exposes the default max wait applied when Config.MaxWait <= 0.
const DefaultMaxWait = 500
